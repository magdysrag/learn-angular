import { ArabicTsDirective } from './arabic.ts.directive';

describe('ArabicTsDirective', () => {
  it('should create an instance', () => {
    const directive = new ArabicTsDirective();
    expect(directive).toBeTruthy();
  });
});
