import { InputTestDirective } from './input-test.directive';

describe('InputTestDirective', () => {
  it('should create an instance', () => {
    const directive = new InputTestDirective();
    expect(directive).toBeTruthy();
  });
});
